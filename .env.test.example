###############################################
# Test environment configuration (local stack)
#
# This file provides defaults for integration testing
# using a local docker-compose stack. Copy this to 
# .env.test for test isolation.
#
# The integration tests spin up a LOCAL docker-compose
# stack (see test/integration/docker-compose.yml):
#   - InfluxDB: localhost:8086
#   - Mosquitto (MQTT): localhost:1883
#   - Test nginx proxy (optional): localhost:8081
#
# By keeping these values on localhost, we avoid hitting
# production services during CI runs and ensure the Influx
# health check succeeds quickly.
###############################################

# MQTT (direct broker port – NOT the WS proxy)
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_CLIENT_ID=test_client

# InfluxDB (default to local direct service)
# For CI/local integration use direct container:
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=test-token-for-integration-tests
INFLUX_ORG=test-org
INFLUX_BUCKET=test-bucket

# Flag to help code paths detect test context
TEST_ENV=true

# Video Stream (test default - HLS stream for testing)
HLS_URL=http://localhost:8000/stream.m3u8
